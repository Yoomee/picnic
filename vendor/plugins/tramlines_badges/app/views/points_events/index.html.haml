-title "Points events"

-if @points_events.empty?
  %p There are no points events.
-else
  %ul.lst
    -@points_events.each do |points_event|
      %li
        =points_event.name
        -bracket do
          -separate_links do
            =link_if_allowed 'Edit', edit_points_event_proc(points_event)
            =link_if_allowed 'Delete', points_event, :method => :delete, :confirm => "Are you sure you want to delete this points event?"

-options_panel do
  =back_link
  =link_if_allowed("New points event", new_points_event_proc)