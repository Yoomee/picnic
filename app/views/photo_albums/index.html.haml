-title "System Albums"
-if @system_albums.blank?
  %p There are no system albums.
-else
  .photo_albums
    %dl
      -@system_albums.each do |photo_album|
        %span.photo_album
          %dt
            =photo_album.name
          %dd
            =link_to(image_for(photo_album, "160x160#"), photo_album_photos_path(photo_album))
-options_panel do
  =back_link
  =link_to "New album", new_photo_album_path
