.flip{:class => "x#{x} y#{y}"}
  .padding
    %h6 FESTIVAL 2011
    .text2
      Presenting
      %br
      =flipitem
  -y = 2 if y ==3 
  =link_to(image_or_photo_for(flipitem, "#{x*235+5*(x-1)}x#{2*140+5}#"),flipitem)
