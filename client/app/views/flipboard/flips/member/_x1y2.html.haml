-x = FlipcolTemplate::X1
-y = (FlipcolTemplate::Y1 * 2) + FlipcolTemplate::MARGIN
.flip.x1.y2
  =link_to(image_or_photo_for(flipitem, "#{x}x#{y}#"),flipitem)
  .hover
    .padding
      %h6 FESTIVAL 2011
    .text2
      Presenting
      %br
      =flipitem
    .text3
      =truncate(flipitem.bio, :length => 100)