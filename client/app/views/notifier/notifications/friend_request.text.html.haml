="Hi #{@recipient.forename},"
-important_email_content do
  %p
    ="#{link_to(@friend_request.member.full_name, site_url + member_path(@friend_request.member))} would like to join your picnic.#{@friend_request.message.blank? ? '' : ' They left this message for you:'}"
  -if !@friend_request.message.blank?
    %p="\"#{@friend_request.message}\""
  %p="#{link_to("Click here", site_url + notifications_path)} to view the request."
%p 
  Thanks,
  %br
  ="The #{site_name} team,"
  %br
  =site_url