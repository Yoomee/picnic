-form.inputs do
  =form.input :name
  =form.input :email_address
  =form.input :comments, :as => :text, :input_html => {:rows => 5}
  -if form.object.validate_spam_answer
    =form.input :spam_answer, :as => :hidden
  -else
    =form.input :spam_answer, :label => form.object.spam_question, :help => "Just so we can be sure you are a human PICNICer, please answer the following question:", :input_html => {:value => ''}
      