-title "Venues"
-if @venues.empty?
  %p.intro
    There are no venues currently on the system. Click
    =link_to 'here', new_venue_path
    to create one.
-else
  %p.intro
    The following venues are on the system. Click one to view more details.
  %br.clear
  %ul.lst
    -@venues.each do |venue|
      %li
        =link_to_self venue
        -bracket do
          -separate_links do
            =link_if_allowed('Edit', edit_venue_proc(venue))
            =link_if_allowed('Delete', edit_venue_proc(venue), :method => :delete, :confirm => "Are you sure you want to delete this venue?")
-options_panel do
  =back_link
  =link_if_allowed 'New venue', new_venue_proc